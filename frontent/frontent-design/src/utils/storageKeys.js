export const STORAGE_KEYS = {
  themeId: 'dipcoder.theme.id',
  themeMode: 'dipcoder.theme.mode',
  sections: 'dipcoder.sections',
  authToken: 'dipcoder.auth.token',
  authUser: 'dipcoder.auth.user',
}
